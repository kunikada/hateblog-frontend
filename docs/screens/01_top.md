# トップページ

### 日別データの確認
```
GET /api/v1/archive?min_users=5
```
#### リクエストパラメータ
- `min_users`: 最低ブックマーク件数フィルタ
  - 5を指定する

#### レスポンス
openapi.yamlを参照

## インタラクション

### 初期表示
1. 日別データの確認
2. 最新日付で人気順エントリーのページ表示。ただしURLは変わらない
